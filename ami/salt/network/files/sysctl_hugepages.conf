# Reserve hugepages at boot. Maps to the TLB blog post: every TLB
# entry covers a 2 MB hugepage instead of a 4 KB page, dropping TLB
# misses on the Envoy heap.

vm.nr_hugepages = 1024
vm.hugetlb_shm_group = 0
vm.zone_reclaim_mode = 0
